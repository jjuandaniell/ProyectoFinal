<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Permisos para cámara y almacenamiento -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icononiria"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icononiria_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Oniria"
        tools:targetApi="36">

        <!-- FileProvider para compartir archivos de forma segura -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.oniria.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:theme="@style/Theme.Oniria">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".UserSetupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.Oniria" />

        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.Oniria" />

        <!-- Activity de Gráficos -->
        <activity
            android:name=".ChartsActivity" 
            android:exported="false"
            android:label="Resumen Gráfico"
            android:theme="@style/Theme.Oniria" />

        <activity
            android:name=".AddIncomeActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.Oniria" />

        <activity
            android:name=".AddExpenseActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.Oniria" />

        <activity
            android:name=".EditSalaryActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.Oniria" />

        <activity
            android:name=".ManageGoalsActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.Oniria" />

        <activity
            android:name=".CalendarReminderActivity"
            android:exported="false"
            android:label="Crear Recordatorio"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.Oniria" />

        <activity
            android:name=".ViewRemindersActivity"
            android:exported="false"
            android:label="Mis Recordatorios" 
            android:theme="@style/Theme.Oniria" />

        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:label="Historial"
            android:theme="@style/Theme.Oniria" />

        <activity
            android:name=".AIAssistantActivity"
            android:exported="false"
            android:label="Asistente IA"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.Oniria" />

        <!-- Activity de Captura de Recibos -->
        <activity
            android:name=".ReceiptCaptureActivity"
            android:exported="false"
            android:label="Capturar Recibo"
            android:theme="@style/Theme.Oniria" />

        <receiver android:name=".ReminderNotificationReceiver" android:enabled="true" android:exported="false"/>

    </application>

</manifest>